@model IEnumerable<WfhRequestDto>

@{
    ViewData["Title"] = "Pending My Approval - Work From Home";

    string StatusBadgeClass(int status) => status switch
    {
        (int)EnumExtensions.WfhRequestStatus.Pending => "bg-warning",
        (int)EnumExtensions.WfhRequestStatus.Approved => "bg-success",
        (int)EnumExtensions.WfhRequestStatus.Rejected => "bg-danger",
        (int)EnumExtensions.WfhRequestStatus.Cancelled => "bg-grey",
        _ => "bg-grey"
    };

    string StatusText(WfhRequestDto item) =>
        !string.IsNullOrEmpty(item.StatusName)
            ? item.StatusName
            : ((EnumExtensions.WfhRequestStatus)item.Status).ToString();
}

<div class="panel panel-flat">

    <div class="panel-heading">

        <h5 class="panel-title">
            <i class="icon-checkmark4 position-left"></i>
            Work From Home - Pending My Approval
        </h5>

        <div class="heading-elements">

            <a asp-action="MyRequests" class="btn btn-default">
                <i class="icon-list"></i>
                My Requests
            </a>

        </div>

    </div>

    <div class="table-responsive">

        <table class="table table-bordered table-striped">

            <thead>
                <tr>
                    <th>Employee</th>
                    <th>Department</th>
                    <th>From Date</th>
                    <th>To Date</th>
                    <th>Total Days</th>
                    <th>Reason</th>
                    <th>Status</th>
                    <th width="160">Action</th>
                </tr>
            </thead>

            <tbody>

                @if (Model != null && Model.Any())
                {
                    foreach (var item in Model)
                    {
                        <tr>

                            <td>@item.EmployeeName</td>

                            <td>@(item.DepartmentName ?? "-")</td>

                            <td>@item.FromDate.ToString("dd-MMM-yyyy")</td>

                            <td>@item.ToDate.ToString("dd-MMM-yyyy")</td>

                            <td class="text-center">@item.TotalDays</td>

                            <td>
                                <span class="text-muted text-size-small">
                                    @(item.Reason?.Length > 40 ? item.Reason.Substring(0, 40) + "..." : item.Reason)
                                </span>
                            </td>

                            <td>
                                <span class="label @StatusBadgeClass(item.Status)">@StatusText(item)</span>
                            </td>

                            <td style="padding: 10px 15px;">

                                <div class="btn-group">

                                    <a asp-action="Details"
                                       asp-route-id="@item.Id"
                                       class="btn btn-default btn-icon"
                                       data-popup="tooltip"
                                       title="View">

                                        <i class="icon-eye"></i>

                                    </a>

                                    @if (item.Status == (int)EnumExtensions.WfhRequestStatus.Pending)
                                    {
                                        <button type="button"
                                                class="btn btn-success btn-icon btnApprove"
                                                data-id="@item.Id"
                                                data-popup="tooltip"
                                                title="Approve">

                                            <i class="icon-checkmark4"></i>

                                        </button>

                                        <button type="button"
                                                class="btn btn-danger btn-icon btnReject"
                                                data-id="@item.Id"
                                                data-popup="tooltip"
                                                title="Reject">

                                            <i class="icon-cross2"></i>

                                        </button>
                                    }

                                </div>

                            </td>

                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="8" class="text-center text-muted" style="padding:30px;">
                            <i class="icon-checkmark-circle" style="font-size:32px;"></i><br />
                            Nothing pending your approval.
                        </td>
                    </tr>
                }

            </tbody>

        </table>

    </div>

</div>

<!-- Approve form (no extra input needed - simple hidden-id POST) -->
<form id="approveForm" asp-action="Approve" method="post" style="display:none;">
    <input type="hidden" id="ApproveWfhRequestId" name="id" />
</form>

<partial name="_RejectModal" />

<script src="~/global_assets/js/plugins/notifications/sweet_alert.min.js"></script>

@section Scripts {

    <script>

        @if (TempData["Success"] != null)
        {
            <text>
                swal({
                    title: 'Success',
                    text: '@Html.Raw(TempData["Success"])',
                    type: 'success',
                    confirmButtonColor: '#66BB6A'
                });
            </text>
        }

        @if (TempData["GlobalError"] != null)
        {
            <text>
                swal({
                    title: 'Error',
                    text: '@Html.Raw(TempData["GlobalError"])',
                    type: 'error',
                    confirmButtonColor: '#EF5350'
                });
            </text>
        }

    </script>

    <script>

        $(document).ready(function () {

            $('[data-popup="tooltip"]').tooltip();

            // Approve - confirm, then submit the hidden form.
            $(document).on('click', '.btnApprove', function () {

                var id = $(this).data('id');

                swal({
                    title: 'Approve this request?',
                    text: 'The employee will be marked Work From Home for the requested dates.',
                    type: 'warning',
                    showCancelButton: true,
                    confirmButtonColor: '#66BB6A',
                    confirmButtonText: 'Yes, approve it'
                }, function (isConfirm) {
                    if (isConfirm) {
                        $('#ApproveWfhRequestId').val(id);
                        $('#approveForm').submit();
                    }
                });

            });

            // Reject
            $(document).on('click', '.btnReject', function () {

                var id = $(this).data('id');

                $('#RejectWfhRequestId').val(id);

                $('#rejectModal').modal('show');

            });

        });

    </script>

}
